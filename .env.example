# Server-side AI configuration for diagnosis pipeline and chat
AI_API_KEY=
# Optional custom gateway URL (defaults to OpenRouter or direct OpenAI/Gemini-compatible endpoint)
# AI_GATEWAY_URL=https://openrouter.ai/api/v1/chat/completions
# AI_MODEL=google/gemini-2.5-flash

# Runtime
NODE_ENV=production
PORT=3000
